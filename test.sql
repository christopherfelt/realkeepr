USE realkeeper;

-- insert into keeps
-- (name, description, userId, img, isPrivate, views, 
-- shares, keeps)
-- VALUES
-- ("test entry", "just doing some testing", "yeah", "httpsomething", 0, 0, 0, 0);

select * from keeps;